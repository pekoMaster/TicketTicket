# 任務日誌：修復首頁卡片排序相反的問題

## 1. 任務執行狀態
狀態：已完成 ✅

### 問題根源 (Root Cause)
使用者回報「活動卡片的排序反了，新的在下面，舊的在上面」。
這個問題是由於**前後端預設排序不一致**所導致的：
- 在先前的修改中，我們的後端 (Supabase API) 是依照「建立時間倒序 (`created_at DESC`)」來抓取分頁資料，這樣第一頁才會是「最新建立的卡片」。
- 然而，首頁前端 (`HomePageContent.tsx`) 當時的預設排序還是設定為 `sortBy: 'date'` （依照活動舉辦日期由近到遠排序）。
- 這導致了前端拿到「最新建立的卡片」後，又擅自把它們用「活動舉辦日期」重新大洗牌。結果活動日期近但很舊的卡片跑到了上面，而剛發布的卡片反而被擠到了下面，甚至在載入第二頁時會發生卡片亂跳的詭異情況。

### 解決方案
我已經將前端的預設排序對齊了後端的抓取順序：
1. **修改 `HomePageContent.tsx` 預設排序**：將預設的 `sortBy` 狀態從 `'date'` 改為 `'newest'`。
2. **清除篩選行為同步**：當使用者按下「清除所有篩選條件」時，恢復成 `'newest'` 的預設排序。
3. **過濾器紅點同步**：修改了提示紅點的判斷邏輯，現在只有當使用者**沒有**使用 `'newest'` 排序時，才會將排序計為「作動中的過濾條件」。

## 2. 下一步操作
狀態：等待用戶手動部署...
說明：
經過這番修改，活動卡片現在一載入就會完美的「新的在最上面，舊的在下面」，且動態載入時也會平順的往下長。
請幫忙將 `src/app/_components/HomePageContent.tsx` 再次推送到 Github！
